@model List<Clinic_Management.Models.User>
<h2>Medicine Stock Report</h2>
<h2>Medicine Stock Report</h2>
<h2 class="text-center">Staffs</h2>

<a asp-controller="Admin" asp-action="AddStaff" class="btn btn-primary">Add Staff</a>
<table class="table table-striped table-hover table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var staff in Model)
        {
            @* string role = "";
            if (staff.Staff_Role == 0)
            {
                role = "Receptionist";
            }
            else if (staff.Staff_Role == "1")
            {
                role = "Pharmacist";
            }
            else if (staff.Staff_Role == 2)
            {
                role = "Technician";
            }
            else
            {
                role = "Doctor";
            } *@
            <tr>
                <td>@staff.Id</td>
                <td>@staff.Name</td>
                <td>@staff.Email</td>
                <td>@staff.Staff_Role</td>
            </tr>
        }
    </tbody>
</table>

<script>
    $(document).ready(function () {
    @if (TempData["success"] != null)
    {
        <text>
                            Swal.fire({
                                title: 'Success!',
                            text: '@TempData["success"]',
                            icon: 'success',
                            confirmButtonText: 'Okay'
                                            });
        </text>
    }
    @if (TempData["error"] != null)
    {
        <text>
                                Swal.fire({
                                    title: 'Error!',
                                text: '@TempData["error"]',
                                icon: 'error',
                                confirmButtonText: 'Okay'
                                                });
        </text>
    }
                });
</script>